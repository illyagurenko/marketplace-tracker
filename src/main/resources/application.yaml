spring:
  application:
    name: marketplace-tracker
  datasource:
    url: jdbc:postgresql://localhost:5432/marketplace_tracker_db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
